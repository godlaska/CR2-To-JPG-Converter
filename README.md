# CR2-To-JPG-Converter
Batch-converts Canon CR2 raw files to JPG or HEIC on Windows. Uses dcraw for high-quality 16-bit raw decoding and exiftool to preserve Exif metadata. All dependencies bundled, just add ImageMagick and run from Git Bash.
